import{_ as t,c as r,o,af as i}from"./chunks/framework.4vjtQ5Ee.js";const u=JSON.parse('{"title":"Scripts-First Lifecycle Overview","description":"","frontmatter":{},"headers":[],"relativePath":"patterns/scripts-first-lifecycle-overview.md","filePath":"patterns/scripts-first-lifecycle-overview.md"}'),a={name:"patterns/scripts-first-lifecycle-overview.md"};function s(c,e,n,l,d,p){return o(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="scripts-first-lifecycle-overview" tabindex="-1">Scripts-First Lifecycle Overview <a class="header-anchor" href="#scripts-first-lifecycle-overview" aria-label="Permalink to &quot;Scripts-First Lifecycle Overview&quot;">​</a></h1><ul><li><strong>Audience:</strong> Lane D program leads and Lane B storytellers coordinating rollouts</li><li><strong>Decision Anchor:</strong> <a href="https://github.com/louis-pvs/plaincraft/blob/main/ideas/scripts-firstlifecycle-v3.md" target="_blank" rel="noreferrer">Scripts-First Lifecycle v3</a></li><li><strong>Owner:</strong> @lane.b</li><li><strong>TTL:</strong> Review by 2026-06-30</li><li><strong>Future Metric:</strong> 100% of lifecycle moves include attached dry-run transcripts within project status notes</li><li><strong>Recorded Media:</strong> Record later</li></ul><h2 id="why-it-matters" tabindex="-1">Why it Matters <a class="header-anchor" href="#why-it-matters" aria-label="Permalink to &quot;Why it Matters&quot;">​</a></h2><p>The lifecycle only stays trustworthy when every transition runs through the sanctioned scripts. Intake, branch, PR refresh, and closeout each treat the idea file as the narrative source while Projects hold the live status. This pattern keeps both SoTs aligned and gives stakeholders a predictable audit trail at every step.</p><h2 id="worked-example" tabindex="-1">Worked Example <a class="header-anchor" href="#worked-example" aria-label="Permalink to &quot;Worked Example&quot;">​</a></h2><p>Lane D triages <code>ARCH-scripts-first-automation-suite</code> and records one dry-run transcript per command:</p><ol><li><code>pnpm ops:idea-intake --file ideas/ARCH-scripts-first-automation-suite.md --yes</code> — synchronises Project fields (<code>Lane</code>, <code>Type</code>, <code>Priority</code>, <code>Owner</code>) and moves the card to Ticketed.</li><li><code>pnpm ops:create-branch -- --id ARCH-123 --slug scripts-first-automation --yes</code> — creates <code>feat/ARCH-123-scripts-first-automation</code>, switches local worktree, and moves the Project status to Branched.</li><li><code>pnpm ops:reconcile-status -- --id ARCH-123 --yes</code> — rewrites the idea <code>Status:</code> line from the Project card and confirms the board is tracking the same target lane before review starts.</li><li><code>pnpm ops:open-or-update-pr -- --id ARCH-123 --yes</code> — opens or refreshes the draft PR title/body (Purpose, Problem, Proposal + checklist), adds lifecycle labels, and transitions the Project status to PR Open.</li><li><code>pnpm ops:closeout -- --id ARCH-123 --yes</code> — after merge, archives the idea (unless skipped), appends the changelog entry, and bumps the Project status to Merged.</li></ol><p>Before pushing, the developer runs <code>pnpm commit:guard -- --range origin/main..HEAD</code> to confirm headers are boring and <code>pnpm drift:check</code> so the idea still maps to canonical statuses. If anything drifts, the dry-run output points straight at the fix.</p><h2 id="constraints-to-honour" tabindex="-1">Constraints to Honour <a class="header-anchor" href="#constraints-to-honour" aria-label="Permalink to &quot;Constraints to Honour&quot;">​</a></h2><ul><li>Keep idea frontmatter (<code>Lane</code>, <code>Owner</code>, <code>Priority</code>, <code>Issue</code>, <code>Status</code>) current; automation copies these fields into Projects and PR metadata.</li><li>Treat dry-run output as part of the review artifact. Attach it to project updates before running with <code>--yes</code>.</li><li>Avoid manual Project status edits. If a correction is required, re-run the command so the scripted reconciliation writes the truth.</li><li>Store <code>_tmp/</code> changelog entries and idea archives in source control so closeout remains reproducible.</li><li>Fail any work-in-progress branch until <code>pnpm guardrails</code> reports <code>ok: true</code>; each lane owns fixing its scope before requesting review.</li><li>Guardrails never skip: <code>pnpm guardrails</code> (or the CI job) now calls <code>commit:guard</code>, <code>drift:check</code>, and <code>scripts:lifecycle-smoke</code>. Fix violations locally before retrying the promotion.</li><li>Nightly execute jobs must run <code>pnpm scripts:lifecycle-smoke --execute --sandbox &lt;path&gt;</code> so write-mode checks hit a disposable clone. Keep the sandbox repository clean and authenticated with a bot token that can mutate PRs/Projects.</li></ul><h2 id="recovery-paths" tabindex="-1">Recovery Paths <a class="header-anchor" href="#recovery-paths" aria-label="Permalink to &quot;Recovery Paths&quot;">​</a></h2><ul><li><strong>Script failed?</strong> Re-run with <code>--dry-run --output json</code> to capture the diff plan, fix the underlying issue, then re-run with <code>--yes</code>.</li><li><strong>Commit headers rejected?</strong> Run <code>pnpm commit:guard -- --range origin/main..HEAD --output json</code> to see the offending commit and amend it before pushing.</li><li><strong>Idea status drift?</strong> Run <code>pnpm drift:check --output json</code> to list files missing canonical statuses or lanes, adjust the idea frontmatter, then re-run.</li><li><strong>CI smoke red?</strong> <code>pnpm scripts:lifecycle-smoke --yes</code> reproduces the job locally (idea validation, branch/PR dry-run, baseline refresh) so you can repair the flow without waiting for another pipeline run.</li></ul><h2 id="links" tabindex="-1">Links <a class="header-anchor" href="#links" aria-label="Permalink to &quot;Links&quot;">​</a></h2><ul><li>Template README: <a href="https://github.com/louis-pvs/plaincraft/blob/main/templates/ideas/README.md" target="_blank" rel="noreferrer"><code>templates/ideas/README.md</code></a></li><li>Template USAGE: <a href="https://github.com/louis-pvs/plaincraft/blob/main/templates/script/USAGE.md" target="_blank" rel="noreferrer"><code>templates/script/USAGE.md</code></a></li><li>Storybook Views: <a href="https://louis-pvs.github.io/plaincraft/storybook/?path=/docs/governance-lifecycle-intake--docs" target="_blank" rel="noreferrer">Intake</a>, <a href="https://louis-pvs.github.io/plaincraft/storybook/?path=/docs/governance-lifecycle-branch--docs" target="_blank" rel="noreferrer">Branch</a>, <a href="https://louis-pvs.github.io/plaincraft/storybook/?path=/docs/governance-lifecycle-pr-refresh--docs" target="_blank" rel="noreferrer">PR Refresh</a>, <a href="https://louis-pvs.github.io/plaincraft/storybook/?path=/docs/governance-lifecycle-closeout--docs" target="_blank" rel="noreferrer">Closeout</a></li><li>Lifecycle Overview View: <a href="https://louis-pvs.github.io/plaincraft/storybook/?path=/docs/governance-lifecycle-overview--docs" target="_blank" rel="noreferrer"><code>Governance / Lifecycle Overview</code></a></li></ul>',14)])])}const f=t(a,[["render",s]]);export{u as __pageData,f as default};
